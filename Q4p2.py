#longest sequence from list of sequences
A = [list of sequences]
for each sequence in range 1 to total sequences
     input the length of sequences to total number of sequences
     #Accuulate the total length for total number of sequences 
set average length to total length
if average length > longest average length 
     set longest average seq to average seq length
     set sequence with longest average length
print "seq with the longest length";
seq with the longest length
print"longest seq length" longest seq length