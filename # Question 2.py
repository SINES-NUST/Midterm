# Question 2
# A sales company wants to identify employees whose sales are much lower than the average sales.

import data()
import array amount_earned_by_sales_and_name(i,str)

for i in amount_earned_by_sales:
   len(amount_earned_by_sales)
   average = sum amount_earned_by_sales/len
else:
   return "invalid"

employees_less_sales = []
for i < average:
   return (i, empolyee name)
   employees_less_sales.append(i)
else:
   return "invalid"

print (employees_less_sales)
