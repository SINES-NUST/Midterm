create and inilize the list 'mainlist', 'foodlist', 'transportlist', 'entertainmentlist' 
now loop is added to transfer items in main list to other lists according to their type. 
mainlist=['banana','bread','carwash','tyre','DVD']
iterate through 'mainlist' list length:
    if mainlist[item]=='banana' or 'bread'
     copy the item from main list to store in foodlist
    else if mainlist[item]=='carwash' or 'tyre'
     copy the item from main list to store in transportlist
    else 
     copy the item from main list to store in entertainmentlist

print all lists in the end. 
print ("food list is:", foodlist)
print ("transport list is:", transportlist)
print ("entertainment list is:", entertainmentlist)  