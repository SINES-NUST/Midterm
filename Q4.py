def GC_content(DNAseq_lst):
    for sequence in DNAseq_lst:
        count(G)
        count(C)
        percentage = (G+C/len(sequence))*100
        append percentage to percentage_lst (percentage_lst.append(percentage))
print (percentage_lst)
GC_content(DNAseq_lst)  # calling the function



for sequence in DNAseq_lst:
    longest = 0
    long_sequence = sequence
    if DNAseq_lst(len(sequence)) > longest:
        then assign that len(sequence) to longest
        assign that sequence to long_sequence
print (len(sequence))
print (long_sequence)