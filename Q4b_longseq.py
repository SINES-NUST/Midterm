#load file containing all sequences
all_sequences= load file containing the sequences
#initialize variables
longest_sequence= set longest_sequence to first all_sequences
for i in range (all_sequences):
    if length of all_sequences at i > length of longest_sequence:
        longest_sequence= all_sequences at i
    End if
End for
#output results
print The Longest Sequence is: longest_sequence
#something