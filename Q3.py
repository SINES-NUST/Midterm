# Given daily expenses list and categorized lists
Expense_list = ["Apple", "Bus", "Movie", "Banana"]
# initialize a lists to store or categorized the items
Food = []
Transport = []
Entertainment = []
# Iterate through each item in the expense_list
for items in Expense_list:
    # check if the item is not properly categorized
    if item in the Food
        # Add item to the respected category
        # Food. append ("Apple"n "Banana")
        Food. append (item)
        # Loop will iterate and then again append in the other two categories
        Trasport. append (item) # append (Bus)
        Entertainment.append (item) # append (Movie)
# Display or return the items in the categories
    if Food Transport Entertainment:
        print ("Food items", Food) # "apple", "banana"
        print ("Bus Transportation", Transport)
        print ("Movie", Entertainment)
    else:
        print ("Have been categorized")