Initialize
Daily expenses = ["food", "transport", "Enterytainment"]
#we have to make a list of daily expenses
#print the list
print ("Daily expenses")
set total expenses to 0
set number of expenses to the length of expenses
for each expense in expense list:
    Add expense to total expenses
    set average expenses to total expenses/number of expenses
    return average expenses
Function determine expenses (average expenses)
determine the daily expenses based on thew average expenses
#we have the average expense now
For each expense in expense list:
    Extract expense for each single item
    if expense > 5000
        return "More expensive"
    Else if average expenses > 2000
        return "It's ok you can manage it"
    Else if average expenses > 1000
         return ("reasobale expense")
     else
            not expensive